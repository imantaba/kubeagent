# Compiled binary
/kubeagent
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary and coverage output
*.test
*.out
coverage.*

# Release artifacts (produced by the release workflow / local dry-runs)
*.tar.gz
SHA256SUMS

# Go workspace files
go.work
go.work.sum

# Vendored deps (we use modules)
vendor/

# Editor / IDE
.idea/
.vscode/
*.swp

# Secrets — never commit an API key or kubeconfig
.env
*.kubeconfig
kubeconfig

# Claude Code local session settings
.claude/

# Local Go-learning companion (kept on disk, not tracked)
docs/go-concepts.md
