{
  "$comment": "additionalProperties is deliberately unset (JSON Schema's default: permitted). A MINOR version bump may add properties, and a document must still validate against an older schema of the same MAJOR.",
  "$defs": {
    "rbacprofile.Rule": {
      "$comment": "additionalProperties is deliberately unset (JSON Schema's default: permitted). A MINOR version bump may add properties, and a document must still validate against an older schema of the same MAJOR.",
      "properties": {
        "apiGroup": {
          "type": "string"
        },
        "nonResourceURLs": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "resources": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "verbs": {
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        }
      },
      "required": [
        "apiGroup",
        "verbs"
      ],
      "type": "object"
    }
  },
  "$id": "https://k8sproject.top/schemas/rbac-print-v1.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "The document written by `kubeagent rbac print --output json`: the ClusterRole name and the least-privilege rules the selected features need.",
  "properties": {
    "roleName": {
      "type": "string"
    },
    "rules": {
      "items": {
        "$ref": "#/$defs/rbacprofile.Rule"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "schemaVersion": {
      "description": "This document's schema version, MAJOR.MINOR. Treat an unknown MINOR as compatible.",
      "pattern": "^1\\.[0-9]+$",
      "type": "string"
    }
  },
  "required": [
    "roleName",
    "rules",
    "schemaVersion"
  ],
  "title": "kubeagent RBAC rules",
  "type": "object"
}
